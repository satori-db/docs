---
title: "Installing"
---

Let's get started with satori

1. First, download

   ### Linux

   ```bash
   bash -c "$(curl -fsSL https://example.com/setup.sh)"
   ```

   ### Windows

   ```bash
   powershell -ExecutionPolicy Bypass -NoProfile -Command "Invoke-WebRequest -Uri 'https://example.com/setup.ps1' -OutFile 'setup.ps1'; .\setup.ps1"
   ```
2. Then run satori

   ```bash
   satori -a root admin
   ```
3. Download our SDKs for communicating with the Satori Runtime

<CardGroup cols={2}>
  <Card title="SDKs" icon="code" color="#2286df">
    
  </Card>
</CardGroup>